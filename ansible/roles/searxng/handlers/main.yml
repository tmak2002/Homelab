---
- name: Restart searxng
  ansible.builtin.command:
    chdir: /opt/searxng
    cmd: "{{ docker_update_command }}"

- name: Restart caddy
  ansible.builtin.command:
    cmd: "{{ caddy_restart_command }}"
