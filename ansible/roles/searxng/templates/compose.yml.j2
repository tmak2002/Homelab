services:
  redis:
    container_name: searxng_redis
    image: redis:7.2.4-alpine
    restart: unless-stopped
    command: redis-server --save 30 1 --loglevel warning
    volumes:
      - /srv/searxng/redis-data:/data
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    networks:
      - searxng

  searxng:
    container_name: searxng
    image: searxng/searxng@sha256:c560279d6f5f4d976587d0065c3eb8e2a26bcb8213b0f76c11384a4c003c5bd9
    restart: unless-stopped
    volumes:
      - /srv/searxng/settings.yml:/etc/searxng/settings.yml
      - /srv/searxng/limiter.toml:/etc/searxng/limiter.toml
    environment:
      - SEARXNG_BASE_URL=https://searxng.{{ services_domain }}
      - SEARXNG_REDIS_URL=redis://searxng_redis
      - SEARXNG_SECRET="{{ searxng_secret }}"
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "1"
    networks:
      - services
      - searxng

networks:
  services:
    external: true
  searxng:
