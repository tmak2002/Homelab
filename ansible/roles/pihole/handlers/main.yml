---
- name: Restart pihole
  ansible.builtin.command:
    chdir: /opt/pihole
    cmd: "{{ docker_update_command }}"

- name: Restart caddy
  ansible.builtin.command:
    cmd: "{{ caddy_restart_command }}"
