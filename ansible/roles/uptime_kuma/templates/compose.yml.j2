services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    restart: always
    container_name: uptime-kuma
    volumes:
      - /srv/uptime-kuma/data:/app/data
    networks:
      - home

networks:
  home:
    external: true
