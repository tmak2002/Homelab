services:
  dockdns:
    container_name: dyndns
    image: ghcr.io/tarow/dockdns:latest
    restart: unless-stopped
    volumes:
      - ./config.enc.yaml:/app/config.yaml
      - /var/run/docker.sock:/var/run/docker.sock:ro